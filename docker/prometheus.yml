global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'datn-backend'
    static_configs:
      - targets: ['host.docker.internal:5000']  # Backend running on host
    metrics_path: '/metrics'
    scrape_interval: 10s
